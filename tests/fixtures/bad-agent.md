---
name: bad-agent
description: This agent fixture is missing the required layer and model fields.
---

# Bad Agent

This fixture must FAIL checkAgentFrontmatter: it has no `layer` field and no
`model` field, which the agent-frontmatter invariant requires.
