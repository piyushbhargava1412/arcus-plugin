# AGENTS.md — arcus-plugin

> Agent navigation index. Generated by `repo-agentifier` from `.context/`.
> **Generated**: 2026-06-18

Navigation index into the shared `.context/` snapshot — the source of truth for how this repository
is built, run, tested, and structured. Follow only the links the current task needs; do not read
linked files up front.

## Project Context

arcus-plugin is a plugin marketplace repository for ARCUS: orchestrator skills, supporting skills,
deterministic helper scripts, and docs for a Spec -> Code -> Pull Request workflow across Copilot CLI,
Claude Code, and VS Code.

One `arcus-controller` orchestrator drives the pipeline over a single ordered set of checkpoint stage
keys (`scaffold -> context_pack -> spec_finalizer -> plan -> test_plan -> branch -> task_1..N ->
code_review -> context_sync -> closure`) in two modes — `interactive` (gated) and `autonomous` (afk) —
that share the sequence and differ only in gating. `implementation-runner` drives the Implementation
loop. Mode triggers, gates, and per-stage execution: see the `arcus-controller` skill body.

### Architecture: two surfaces × three tiers

Capabilities are organized on two orthogonal axes. Read the linked file before reasoning about either
axis — do not infer the rules from the summary below.

- **Surface** = where it lives and who invokes it. **Skills** (`plugins/arcus/skills/<name>/SKILL.md`)
  are user- and model-invocable as `/arcus:<name>`; **agents** (`plugins/arcus/agents/<name>.md`) are
  model-only, dispatched by name, never user-facing. Surface rules and agent frontmatter:
  [`plugins/arcus/agents.md`](plugins/arcus/agents.md). Roster of names + tiers:
  [`agents.md#roster`](plugins/arcus/agents.md#roster); skills are the dirs under
  `plugins/arcus/skills/`.
- **Tier** (`layer:` frontmatter, independent of surface) = role + execution: **capability** (atomic,
  stateless, **spawned** as an isolated subagent), **coordinator** / **orchestrator** (sequencer /
  stateful pipeline driver, both **run in the invoking thread**), **substrate** (reference only). The
  full tier table, the depth-1 execution rule, and the plug-n-play contract convention:
  [`site/concepts/capability-library.md`](site/concepts/capability-library.md).

When resolving a cross-reference, treat the **union** of `skills/` + `agents/` as one namespace
(`walkAll()` in `tests/lib/skills.mjs`).

- Technical map: [.context/repo_map.md](.context/repo_map.md)
- Business scope (purpose, responsibilities, repo invariants): [.context/repo_scope.md](.context/repo_scope.md)

## Navigation Index

| I need to know about… | Go to |
|------------------------|-------|
| Tech stack & versions | [repo_map.md#tech-stack](.context/repo_map.md#tech-stack) |
| Directory layout | [repo_map.md#top-level-structure](.context/repo_map.md#top-level-structure) |
| Entry points | [repo_map.md#entry-surface-locations](.context/repo_map.md#entry-surface-locations) |
| Key components | [repo_map.md#key-packages--modules](.context/repo_map.md#key-packages--modules) |
| Contracts & schemas | [repo_scope.md#evidence-gaps--not-found](.context/repo_scope.md#evidence-gaps--not-found) |
| Configuration | [repo_map.md#config-hotspots](.context/repo_map.md#config-hotspots) |
| Build & run commands | [repo_map.md#scripts--automation](.context/repo_map.md#scripts--automation) |
| Test locations | [repo_map.md#test-locations](.context/repo_map.md#test-locations) |
| Business capabilities | [repo_scope.md#core-responsibilities](.context/repo_scope.md#core-responsibilities) |
| Events (produced/consumed) | [repo_scope.md#evidence-gaps--not-found](.context/repo_scope.md#evidence-gaps--not-found) |
| APIs (exposed/consumed) | [repo_scope.md#evidence-gaps--not-found](.context/repo_scope.md#evidence-gaps--not-found) |
| Data ownership | [repo_scope.md#major-implementation-areas](.context/repo_scope.md#major-implementation-areas) |
| Dependencies | [repo_map.md#tech-stack](.context/repo_map.md#tech-stack) |
| Testing conventions | [testing-patterns.md](.context/testing-patterns.md) |
| Design & coding patterns | [design-and-coding-patterns.md](.context/design-and-coding-patterns.md) |

## Business Flows

Each flow below is documented in its own file under `.context/flows/`. **Load only the flow
relevant to your current task.**

- [Checkpoint Lifecycle State Management](.context/flows/checkpoint-lifecycle-state-management.md)
- [Docs Build And Pages Deploy](.context/flows/docs-build-and-pages-deploy.md)
- [Pull Request Creation And Publish](.context/flows/pull-request-creation-and-publish.md)
- [Session Bootstrap Hook](.context/flows/session-bootstrap-hook.md)
- [Story Branch Workspace Initialization](.context/flows/story-branch-workspace-initialization.md)
- [Story Commit Persistence](.context/flows/story-commit-persistence.md)

## Working Agreement

- Treat `.context/` as authoritative; if it is stale, re-run `repo-agentifier` to refresh it.
- Ground every change in repository evidence — do not infer behavior not present in the codebase.
- Keep this index in sync: regenerate it whenever the repository structure changes materially.
- **Keep the plugin version live (semver, `plugins/arcus/.claude-plugin/plugin.json`).** After every
  interaction that materially changes the plugin, evaluate the bump, **apply it in the same turn**, log
  the change under `CHANGELOG.md` `[Unreleased]`, and state what you did (e.g. "bumped 1.5.2 → 1.5.3
  (patch)"). This keeps `plugin.json` consistent with the working tree so whoever commits — human or
  model — gets a correct version without a follow-up bump commit. Non-material turns (questions,
  exploration, reverted edits) bump nothing.
  - The contract surface that semver protects: **user triggers / slash commands, the skill+agent
    roster and names, inter-skill contracts (checkpoint stage keys, artifact filenames, emitted
    milestone/handoff tokens), and helper-script CLIs.**
  - **major** — backward-incompatible contract change: removed/renamed skill or trigger, changed/reordered
    checkpoint stage key, renamed artifact, breaking helper-script CLI or emitted-token change.
  - **minor** — backward-compatible addition: new skill/agent, trigger alias, optional script flag, or
    pipeline stage.
  - **patch** — no contract-surface change: prompt/wording edits, bug fixes, refactors, docs, tests.
  - Heuristic: forces a `tests/e2e/triggers/corpus.json` change → ≥ minor (removed trigger → major);
    changes a contractual-token assertion → major; neither → patch.
  - **Bump only once per accumulated release** — do not stack per-prompt. If `[Unreleased]` is empty, the
    current version is shipped: the change bumps it and opens `[Unreleased]`. If `[Unreleased]` already
    has entries, the version is pending: bump only when the new change is a **higher** level than what is
    already pending (else just add a changelog line). On release, promote `[Unreleased]` → `[x.y.z] - <date>`.
