# PR: [STORY-ID] - [STORY-TITLE]

<!-- BUDGET: the finished description is <= 80 lines and <= 4 KB. Omit any section
     with nothing worth saying rather than padding it. The reviewer has the diff;
     give them intent, not a transcript of the pipeline. -->

## Description
[2-4 sentences: what this changes and why. Summarize the acceptance criteria — do not restate them.]

## Technical Changes
[3-6 bullets, one line each. Group by area, not by task and not by file.]
- **Feature**: [Key logic added/modified]
- **Tests**: [What is now covered, in a phrase]
- **Architecture**: [Only if a notable pattern or trade-off was applied — detail lives in `plan.md`]

## Review Notes
[Optional, 0-3 bullets: the decisions a reviewer could reasonably disagree with, and where to look
hardest. Skip the section entirely if there are none. Full rationale is in `grounded-spec.md` and
`review.md`.]

## Context Updates
[Include this section ONLY when the branch changed `.context/**` or `AGENTS.md` (the Context Sync
stage ran and reconciled shared context). Render from the sync commit body's `Updated:` lines.
Omit this entire section when no context artifacts changed.]
- **[.context/<artifact>]**: [what changed / which trigger crossed — from the sync commit body]

## Verification
[One or two lines: counts and outcome, never a per-case table — the detail is in `test-plan.md`.]
- [N tests, all passing | N added, M modified] — see `test-plan.md`

---
*This PR was generated by ARCUS.*
